# PythonAnywhere web app runtime.
#
# Keep this file app-specific. Do not replace it with a full
# PythonAnywhere/global "pip freeze"; the server's installed packages are not
# the source of truth for this app.
#
# Compatibility constraints in the current backend:
# - @jwt_required is used without parentheses, so Flask-JWT-Extended must stay
#   on 3.x until the routes are migrated.
# - db.engine.execute(...) is used throughout the backend and scheduled task,
#   so SQLAlchemy must stay below 2.x until those calls are migrated.
# - Jinja2 2.x needs MarkupSafe<2.1. MarkupSafe 2.1+ removed soft_unicode,
#   which is what broke the PythonAnywhere deployment in June 2026.

Flask==1.1.4
Werkzeug==1.0.1
Jinja2==2.11.3
MarkupSafe==2.0.1
itsdangerous==1.1.0
click==7.1.2

Flask-Cors==3.0.10
Flask-JWT-Extended==3.25.1
PyJWT==1.7.1

Flask-SQLAlchemy==2.4.4
SQLAlchemy==1.3.24
Flask-Migrate==2.5.3
alembic==1.4.3

mysql-connector-python==8.0.33
sendgrid==6.11.0
